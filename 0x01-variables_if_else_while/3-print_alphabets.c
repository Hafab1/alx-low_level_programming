#include <stdio.h>
/**
* main - prints the alphabet in lowercase,
* 
* Return: Always 0 (Success)
*/

int main(void)
{
	char c;
	char d;

	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(c <= 'Z') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
